# NEWSCP 从零开始开发计划

> 版本：v1.4 | 更新时间：2026-04-15
> 背景：1 人 + AI，1～2 个月，验证 AI 开发路线可行性
> 原则：以最小时间成本做出可用产品；旧产品只用于参考业务逻辑，不做对接

---

## 一、技术选型（定死，不要在这上面浪费时间）

| 层 | 选择 | 理由 |
|----|------|------|
| 前端框架 | React + Vite + TypeScript | shadcn 官方基础，生态最全 |
| UI 组件 | shadcn/ui + Tailwind CSS | AI 生成代码质量最高的组件库 |
| 状态/请求 | TanStack Query + Axios | 服务端状态管理，少写很多同步代码 |
| 表单 | React Hook Form + Zod | 类型安全，AI 生成代码稳定 |
| 路由 | React Router v6 | 标准 |
| 图表 | Recharts | shadcn 官方推荐，与 Tailwind 一致 |
| 后端框架 | Spring Boot 3 + Hibernate（Spring Data JPA） | 统一实体建模，主数据前完成 ORM 迁移 |
| 数据库 | MySQL（沿用） | 表结构重新设计 |
| 接口规范 | RESTful，统一响应 `{ code, data, msg }` | — |

---

## 二、数据库设计原则（开发前定好，改起来最贵）

### 2.1 表设计模式

**所有业务表必须有的公共字段：**
```sql
id          BIGINT PRIMARY KEY AUTO_INCREMENT
tenant_id   VARCHAR(64)   -- 租户隔离（多租户场景）
created_by  VARCHAR(64)
created_at  DATETIME
updated_by  VARCHAR(64)
updated_at  DATETIME
deleted     TINYINT(1) DEFAULT 0  -- 逻辑删除
```

**头行表结构（适用于单据类：订单、预测版本等）：**
```
xxx_header  -- 单据头（ID、状态、时间范围、备注等）
xxx_line    -- 明细行（关联 header_id，存产品/数量等）
```

**扩展属性表（适用于字段不固定的场景，谨慎使用）：**
```
xxx_attr（entity_id, attr_key, attr_value, attr_type）
```
> 注：不是所有表都要用 EAV。简单配置表用宽表没问题；只有字段真的无法预知时才用 attr 扩展。

### 2.2 命名规范

- 表前缀统一用 `nscp_`（new scp）
- 格式：`nscp_<领域>_<实体>`，例如 `nscp_ord_customer`、`nscp_sto_safety_config`
- 字段用下划线命名法

---

## 三、项目结构（前端）

```
NEWSCP/frontend/
├── src/
│   ├── api/          # 所有接口调用，按域分文件（order.ts / forecast.ts 等）
│   ├── components/   # 通用组件（shadcn 扩展 + 业务通用）
│   ├── pages/        # 页面，按功能域分目录
│   │   ├── customer/
│   │   ├── forecast/
│   │   ├── safety-stock/
│   │   └── ...
│   ├── hooks/        # 通用 hooks
│   ├── stores/       # 全局状态（Zustand，只放真正全局的东西）
│   ├── types/        # TypeScript 类型定义
│   └── lib/          # 工具函数
```

---

## 四、开发计划

> **唯一权威**：阶段 0～12、日历工期与依赖关系均以本节为准。第九章 **9.1～9.4** 为通知、审批、低代码图表、配置驱动等方案的展开说明，阶段对应关系见下文依赖图。

### 4.1 依赖关系图

```
环境搭建
  └→ 系统基础（登录/用户/角色/权限/组织架构）
       └→ 平台与安全底座补强（租户/菜单运营/审计/密码策略/数据权限等，对齐 IPS 缺口）
            └→ 系统配置中心（枚举字典/系统参数）
                 ├→ 通知系统
                 │    └→ 审批流（依赖用户+组织+通知）
                 │         └→ [业务模块按需接入审批]
                 ├→ 低代码图表框架（不依赖业务数据）
                 │    └→ [随各业务域同步注册 VIEW]
                 └→ ORM 迁移（MyBatis-Plus → Hibernate，强制门槛）
                      └→ 主数据（产品/库存点/客户）
                      └→ 业务配置（安全库存配置/优先级因子/净需求规则）
                           └→ 业务数据（订单/预测/安全库存）
                                └→ 高级功能（优先级/协同预测）
                                     └→ 看板搭建（基于图表工具）
                                          └→ 收尾打磨
```

### 4.2 分阶段说明与工期

#### 阶段 0：环境搭建（Day 1～3）

- 前端：React + Vite + TS + shadcn/ui 初始化，Hello World 跑通
- 后端：Spring Boot 3 项目骨架，先跑通认证与权限，主数据前迁移到 Hibernate
- 数据库：`nscp_dev` 库建好，连通前后端
- 全局视觉 Token 与 Dark Neon 主题变量初始化（先统一深色基线，再评估 Light 主题）

**完成标志**：前端调后端接口成功，Dark Neon 全局样式变量可复用，登录页风格可作为全站模板。

**已落地（截至 2026-04-15）**：前后端骨架（Vite + React + TS / Spring Boot + Gradle）、shadcn/ui 基础、统一响应 `{ code, data, msg }`、CORS、`/health`、本地库连接；后端实际使用 Spring Boot 3.5.x（非原计划 3.3.x，无负面影响）。Dark Neon 已在登录页样板落地；**现状说明**：代码仍有 MyBatis-Plus 存量实现，需在阶段 7（主数据）前完成 Hibernate 迁移。

---

#### 阶段 1：系统基础（Day 4～13）

> 所有后续功能的前提，必须最先完成。

**1.1 认证与用户（Day 4～7）**  
登录页 + JWT（`tenant_id`、`user_id`、`roles` 写入 token）；用户 CRUD；密码修改、个人资料。

**1.2 角色与权限（Day 8～10）**  
角色 CRUD；菜单/按钮权限树；用户-角色关联；接口 `@PreAuthorize`。

**1.3 组织架构（Day 11～13）**  
部门树 CRUD；用户-部门关联；直属上级关系（供审批流）。

```sql
nscp_sys_user（id, tenant_id, username, password_hash, real_name, email, phone, status）
nscp_sys_role / nscp_sys_permission / nscp_sys_role_permission / nscp_sys_user_role
nscp_sys_dept / nscp_sys_user_dept
```

**完成标志**：管理员可在界面管理用户/角色/权限/组织，登录后菜单按权限显示。

**进度说明（截至 2026-04-15）**：JWT 登录、主布局、动态菜单、`stage1_init.sql` 种子数据、用户/角色/部门管理页与 DB 认证、`sys:*` 接口权限等已按本阶段目标在仓库中落地；细部以代码与 `设计文档库/阶段1_系统基础/` 为准。

**衔接说明**：阶段 2（平台与安全底座补强）与阶段 3（系统配置中心）建议在阶段 1 验收后再启动；阶段 3 依赖阶段 2 的租户与菜单运营能力就绪，避免管理入口与多租户数据反复重构。

---

#### 阶段 2：平台与安全底座补强（Day 14～21）

> 对照 scp-foundation IPS 补齐缺口；依赖阶段 1。

**2.1 租户与开通（P0）**  
租户 CRUD；用户-多租户关系；（可选）租户-模块/能力开关。

**2.2 菜单与权限运营（P0）**  
基于 `nscp_sys_permission`（或等价模型）在线维护树与排序，与角色分配联动。

**2.3 数据权限（P1，可分迭代）**  
行级/规则型模型与接口；第一期可表结构 + 管理 UI 骨架 + 一类对象试点。

**2.4 密码与安全策略（P1）**  
可配置复杂度/有效期等，与登录失败锁定、强制改密衔接。

**2.5 审计与日志（P1）**  
登录日志；关键管理接口操作审计（对标 `@OperationLog`）。

**2.6 认证加固（P2）**  
Refresh Token、验证码、OAuth/SSO 预留或单协议对接（工作量另估）。

**2.7 前端枚举值映射治理（P0）**  
建立“内部值（value）/显示值（label）”统一映射管理，避免页面散落硬编码与占位值泄漏：
- 统一映射出口：`src/constants`（或等价目录）集中定义 `value-label` 映射与类型
- 页面下拉/单选只消费映射源，不在页面内临时拼接字符串常量
- 约束命名：内部值使用稳定语义值（如 `ALL`、`ENABLED`），禁止 `__ALL__` 这类临时占位命名进入 UI
- 接口层只传内部值，展示层只读显示值；禁止混用导致“显示文案被当业务值”
- 对接字典接口时采用“后端字典优先 + 前端兜底映射”

**2.8 ORM 迁移（P0，主数据前置门槛）**  
在进入阶段 7（主数据）前，必须完成后端 ORM 从 MyBatis-Plus 迁移至 Hibernate（Spring Data JPA）：
- 迁移范围：用户/角色/权限/部门/租户等阶段 1~2 核心模型的持久化层
- 迁移目标：Repository + Entity + 统一审计字段（`created_at/updated_at`）方案跑通
- 多租户要求：Hibernate 层实现 `tenant_id` 自动约束（Filter/Specification/Repository 基类等统一机制）
- 验收门槛：阶段 1~2 已有接口回归通过，且主数据模块不再引入新的 MyBatis-Plus 依赖

**完成标志**：租户与菜单权限可运营；登录与关键操作可追溯；数据权限可扩展或已试点；密码策略可配置。

**进度说明（截至 2026-04-15）**：
- 已完成：2.1 租户与开通（租户 CRUD、用户-租户关联、超管 `admin` 租户、`SUPER_ADMIN_ROLE`/`TENANT_ADMIN_ROLE`、跨租户请求头切换与校验）
- 已完成：2.2 菜单与权限运营（权限树在线运营 API + 页面、角色联动、路由接入）
- 已落地（提前于 2.4）：登录失败锁定与解锁窗口（固定阈值与时长），但“策略可配置化”尚未完成
- 进行中：2.7 前端枚举值映射治理（规则已定义，页面已开始替换，尚未形成统一 `constants` 映射出口）
- 未开始：2.8 ORM 迁移（MyBatis-Plus → Hibernate）
- 未开始：2.3 数据权限、2.5 审计与日志、2.6 认证加固（Refresh Token/验证码/OAuth/SSO）

---

#### 阶段 3：系统配置中心（Day 22～24）

> 业务下拉与全局参数来源；须在业务功能之前完成。

- 枚举字典：`dict_type` + `dict_value`，界面维护
- 系统参数：`param_key` / `param_value` 等

```sql
nscp_sys_dict_type / nscp_sys_dict_value / nscp_sys_param
```

**典型字典**：`ORDER_STATUS`、`FORECAST_STATUS`、`PRODUCT_TYPE`、`STOCK_POINT_TYPE`、`APPROVAL_STATUS` 等（开发初始化，管理员可扩展）。

**完成标志**：前端下拉从字典接口读取，非硬编码。

---

#### 阶段 4：通知系统（Day 25～28）

> 审批流前置依赖。

- WebSocket（Spring + STOMP）；系统通知存库 + 推送 + 未读 Bell；基础私信。

```sql
nscp_sys_notification / nscp_msg_message / nscp_msg_conversation
```

**完成标志**：发通知后 Bell 实时未读，列表可阅。

---

#### 阶段 5：审批流（Day 29～38）

> 依赖：阶段 1 + 阶段 4。

- Flowable Starter、`WorkflowService`；模板管理 UI；待办/已办；同意/拒绝/退回/意见；时间线；动作触发通知。BPMN 由配置生成。

**完成标志**：两级审批模板端到端 + 通知 + 时间线。

---

#### 阶段 6：低代码图表框架（Day 39～45）

> 不依赖业务数据；VIEW 随业务域注册。

- `nscp_chart_datasource` 注册 VIEW；维度/指标/筛选配置；图表 UI；看板拖拽（如 react-grid-layout）；安全 SQL 与白名单。

**完成标志**：测试 VIEW 可配出图并保存看板。

---

#### 阶段 7：主数据（Day 46～53）

> DP 业务基础；须在业务功能之前。**前置门槛：阶段 2.8 ORM 迁移完成。**

- 产品、库存点、产品-库存点关联、客户；字典驱动类型；每表 CRUD + 分页/搜索；每表同步注册图表 VIEW。

**参考旧表**：`mds_pro_product_base`、`mds_pro_stock_point`、`dps_ord_customer` 等。

**完成标志**：三类主数据可维护，VIEW 已注册。

---

#### 阶段 8：业务配置功能（Day 54～64）

> 依赖主数据；配置驱动核心层。

- 安全库存配置、优先级因子、库存点需求比例、净需求规则；改配置即生效；每域注册 VIEW。

**参考旧表**：`dps_sto_safety_stock_config`、`dps_pri_priority_factor_config`、`dps_sto_stock_point_demand_ratio`、`dps_net_netting_rule` 等。

**完成标志**：配置可界面维护且逻辑即时生效。

---

#### 阶段 9：核心业务数据（Day 65～78）

> 依赖阶段 7 + 阶段 8。

- 客户订单（头行、Excel 导入）；需求预测版本与明细；安全库存结果；库存点需求汇总；按域注册 VIEW；预测发布可接审批。

**完成标志**：订单→预测→审批→安全库存链路可演示。

---

#### 阶段 10：高级功能（Day 79～86）

> 依赖阶段 9。

- 需求优先级、意向订单、协同预测（简版）；按域注册 VIEW。

**完成标志**：优先级可算可展。

---

#### 阶段 11：看板搭建（Day 87～90）

> 各域 VIEW 已齐，以配置为主。

- DP 核心看板（预测分析、安全库存达成、订单履行等）；验证低代码端到端。

**完成标志**：≥3 张有意义图表展示真实数据。

---

#### 阶段 12：收尾打磨（Day 91～96）

- 全流程测试；错误与空态；响应式（≥1280px）；《功能演示说明》。

---

### 4.3 阶段状态总览（截至 2026-04-15）

| 阶段 | 状态 | 说明 |
|------|------|------|
| 0 环境搭建 | 已完成 | 前后端骨架、统一响应、健康检查、Dark Neon 基线已落地 |
| 1 系统基础 | 已完成 | 用户/角色/部门/权限核心链路已可运行 |
| 2 平台与安全底座补强 | 部分完成 | P0（2.1/2.2）完成；2.7 进行中；2.3/2.5/2.6 未启动 |
| 3 系统配置中心 | 未开始 | 字典与参数中心尚未落地 |
| 4 通知系统 | 未开始 | 无 WebSocket/通知存储与 Bell 未读 |
| 5 审批流 | 未开始 | 无 Flowable/模板/待办已办链路 |
| 6 低代码图表框架 | 未开始 | 无 datasource/view 注册与图表配置器 |
| ORM 迁移（2.8） | 未开始 | 主数据前置门槛（MyBatis-Plus → Hibernate） |
| 7 主数据 | 未开始 | 主数据模块当前为占位页 |
| 8 业务配置功能 | 未开始 | 依赖阶段 7 |
| 9 核心业务数据 | 未开始 | 依赖阶段 7+8 |
| 10 高级功能 | 未开始 | 依赖阶段 9 |
| 11 看板搭建 | 未开始 | 依赖阶段 6+7+8+9 |
| 12 收尾打磨 | 未开始 | 收口阶段 |

---

### 4.4 总工期汇总

**总工期：约 100 天（约 3.3 个月）**

| 阶段 | 内容 | 天数 |
|------|------|------|
| 0 | 环境搭建 | 3 |
| 1 | 系统基础（用户/权限/组织） | 10 |
| 2 | 平台与安全底座补强 | 8 |
| 3 | 系统配置中心（枚举/字典） | 3 |
| 4 | 通知系统 | 4 |
| 5 | 审批流 | 10 |
| 6 | 低代码图表框架 | 7 |
| 2.8 | ORM 迁移（MyBatis-Plus → Hibernate） | 4 |
| 7 | 主数据 | 8 |
| 8 | 业务配置功能 | 11 |
| 9 | 核心业务数据 | 14 |
| 10 | 高级功能 | 8 |
| 11 | 看板搭建 | 4 |
| 12 | 收尾打磨 | 6 |
| **合计** | | **100** |

> 可压缩：阶段 10（协同预测可砍）、阶段 11（看板可简化），最短约 83 天；阶段 2 中数据权限/OAuth 可降为「仅预留」。

---

## 五、AI 开发工作方式

### 5.1 每个功能域的标准流程

```
1. 先读旧代码（旧表结构 + 旧前端页面逻辑）→ 整理给 AI 做上下文
2. 让 AI 设计新表 DDL → 你审核确认
3. 让 AI 写后端接口（Controller + Service + Entity + Repository）
4. 让 AI 写前端页面（从 shadcn 组件拼装）
5. 联调 → 修 bug → 完成
```

### 5.2 减少返工的关键约定

- **第 0 步定好的技术选型不再变动**，后续不讨论"要不要换 xxx 库"
- **每个功能域开始前**，先把旧产品对应的表结构和页面截图给 AI 看，再让它设计新的
- **接口返回格式**统一，不要一个接口 `{ data: [] }` 一个接口 `{ list: [] }`
- **类型定义（TypeScript）优先**：先写类型，再写组件，AI 生成的代码会更准确
- **不要让 AI 一次写太多**：一次一个页面或一个接口，跑通后再做下一个

### 5.3 用旧产品节省时间的正确姿势

| 借鉴内容 | 怎么用 |
|----------|--------|
| 业务字段定义 | 读旧表结构，理解每个字段含义，设计新表时参考 |
| 计算逻辑 | 读旧 Java 代码，理解规则，让 AI 用新方式实现 |
| 页面交互 | 打开旧前端截图，作为 AI 生成新页面的参考 |
| 接口参数 | 读旧前端的 API 调用，理解入参出参，设计新接口时参考 |

---

## 六、主要风险（只列真实风险）

| 风险 | 应对 |
|------|------|
| 数据库设计早期出错，后期改表成本高 | 第 2 步前把公共字段规范定死；表结构改动在数据量小时（开发期）代价低 |
| AI 生成的代码结构不一致，积累后难维护 | 做完第 1 个功能后提取一个"标准页面模板"，后续都让 AI 照着模板写 |
| 需求范围蔓延，60 天不够 | 严格按上面顺序推进；分析看板和协同功能如果时间不够可以砍掉 |
| shadcn 定制样式耗时 | 第 0 步定好颜色/字体变量，之后不再改全局样式 |
| 多租户隔离漏写，导致数据泄露 | Hibernate 统一租户约束（Filter/Repository 基类）+ 接口层防护双保险 |

---

## 七、第一天就要做的决策（做了就不改）

- [x] 前端项目放 `NEWSCP/frontend/`，后端放 `NEWSCP/backend/`
- [x] 数据库名：`nscp_dev`（开发）
- [x] 端口：前端 `5173`，后端 `8090`
- [x] 主色调：**全站统一 Dark Neon**（黑底 + 霓虹青蓝 + 琥珀点缀），先保证深色专业科技风一致性，再决定是否追加 Light 主题
- [x] 是否做多租户？**YES**，面向 SaaS，租户隔离方案见下文第八节

## 八、全站视觉规范（Dark Neon 执行标准）

> 适用范围：不仅登录页，**主布局、列表、表单、看板、弹窗、审批流页面全部统一执行**。

### 8.1 视觉目标

- 风格关键词：`GitHub-like dark`、`industrial tech`、`clean + high contrast`
- 视觉目标：黑底稳重、亮色聚焦、信息层级清晰，避免“花哨但难用”
- 一致性目标：用户在任何模块都能感知是同一个产品，而不是“每页一个设计”

### 8.2 色彩与字体 Token（全局变量）

- 主背景：`#060913`
- 二级背景：`#090D18`
- 卡片背景：`rgba(9,14,26,0.92)`
- 边框基色：`rgba(148,163,184,0.32)`
- 主亮色（Cyan）：`#06B6D4`
- 次亮色（Blue）：`#38BDF8`
- 点缀色（Amber）：`#F59E0B`
- 主文本：`#F8FAFC`
- 次文本：`rgba(226,232,240,0.84)`
- 弱文本：`rgba(148,163,184,0.9)`
- 错误色：`#FB7185`
- 字体主栈：`'Geist Variable', 'PingFang SC', 'Microsoft YaHei', 'Noto Sans SC', sans-serif`

### 8.3 字体与排版规范（必须统一）

- 字体分工：UI/正文使用主栈，代码与编号可使用等宽字体（仅在需要对齐时）
- 字号梯度（桌面端）：
  - 页面主标题 `40~56px / 700`
  - 模块标题 `24~32px / 650`
  - 卡片标题 `18~22px / 600`
  - 正文 `14~16px / 400~500`
  - 注释与说明 `12~13px / 400`
- 行高规范：
  - 标题 `1.15~1.25`
  - 正文 `1.55~1.7`
  - 表格单元格 `1.4~1.5`
- 字间距规范：
  - 大标题可用 `-0.02em ~ -0.04em`
  - 全大写标签（如状态/Badge）可用 `0.06em ~ 0.12em`
- 数字展示规范：
  - KPI、金额、数量使用 `font-variant-numeric: tabular-nums`
  - 不同页面中的同类数字单位与小数位保持一致
- 移动端适配：
  - 各级标题按比例下调 `10~20%`
  - 最小正文字号不低于 `14px`

### 8.4 组件层规范（必须统一）

- 页面背景：深色渐变 + 弱网格/光斑，不允许纯平涂黑或纯白
- 卡片容器：圆角 `16~24px`，半透明深底，带细边框和轻玻璃感
- 主要按钮：青蓝渐变（可带少量琥珀），悬停抬升 `1~2px`，禁用态降低对比
- 输入框：深底浅边框，Focus 必须有亮色描边（可见性优先）
- 顶栏/侧栏：统一深色体系，当前激活项用亮色底或亮边强调
- 数据卡/KPI：标题弱文本 + 值强对比，禁止大段纯装饰文案

### 8.5 动效与节奏

- 动效原则：少而精，只用于强化反馈与层级
- 推荐动效：背景光斑慢速漂浮、按钮 hover、卡片轻微浮起
- 禁止项：高频闪烁、复杂循环动画、影响阅读的炫技动效

### 8.6 响应式与可用性底线

- 断点：`>=1280` 桌面优先，`<900` 自动堆叠
- 表单可用性：输入高度、点击区域、错误提示在暗色背景下必须清晰
- 对比度：文本与背景保持可读，不为“酷炫”牺牲信息可读性

### 8.7 落地顺序（从登录页扩展到全产品）

1. 登录页（已完成）作为视觉样板
2. 主框架改造：`AppLayout`（侧栏/顶栏/内容容器）统一 Dark Neon
3. 通用组件改造：按钮、输入、表格、分页、弹窗、空状态
4. 页面批量迁移：主数据页 → 配置页 → 业务页 → 看板页
5. 验收：随机抽 5 个页面，检查色彩/间距/状态/交互是否一致

### 8.8 设计红线（避免后续返工）

- 不再新增“页面私有配色方案”，所有颜色必须走全局 Token
- 不允许回退到大面积白底工业表单风
- 不在单页引入与主风格冲突的品牌色（除业务图表配色）

## 九、通用基础设施（三个跨域能力）

> 这三个功能是跨所有业务模块的基础设施，在 **第四章「开发计划」** 中对应阶段 4（通知）、阶段 5（审批）、阶段 6（低代码图表）；整体位于阶段 1 完成之后、阶段 7（主数据）大规模铺开之前分阶段落地。

---

### 9.1 站内通知 + 私信

**目标定位（工业软件合格线）**

| 功能 | 是否实现 | 说明 |
|------|----------|------|
| 通知 Bell + 未读数 | ✅ | 顶栏常驻，实时更新 |
| 系统通知列表 | ✅ | 审批状态变化、系统预警、任务指派 |
| 已读/未读标记 | ✅ | 支持一键全部已读 |
| 基础私信（一对一） | ✅ | 发送/接收/历史记录 |
| 消息推送（实时） | ✅ | WebSocket，页面不刷新也能收到 |
| 群组消息 / @功能 / 已读回执 | ❌ | 超出合格线，不做 |

**数据表设计**

```sql
-- 通知表（系统事件推送）
nscp_sys_notification
  id, tenant_id, receiver_id, type(APPROVAL/ALERT/SYSTEM),
  title, content, related_type, related_id,  -- 关联到具体业务单据
  is_read(0/1), created_at

-- 私信表
nscp_msg_message
  id, tenant_id, sender_id, receiver_id,
  content, is_read(0/1), created_at

-- 私信会话表（聚合两人之间的对话）
nscp_msg_conversation
  id, tenant_id, user_a_id, user_b_id,
  last_message_id, unread_count, updated_at
```

**技术实现**
- 后端：Spring WebSocket（`@EnableWebSocketMessageBroker`）
- 前端：SockJS + STOMP Client，建立连接后订阅个人频道 `/user/{userId}/notification`
- 非实时场景（页面刷新时）：普通 REST 接口拉取未读数和列表

**开发工作量估算：约 4～5 天**

---

### 9.2 审批流 / 工作流

**参考设计**：`产品设计/产品需求/IPS/IPS_审批流能力建设/审批流设计大纲.md`（已有完整设计大纲，含角色体系、功能清单、状态机、与业务模块集成契约）

**使用 Flowable，加一层业务封装**

```
业务代码 → WorkflowService（封装层）→ Flowable API
```

**新产品实现范围（按优先级）**

P0（必须做，核心可用）：
- 流程模板管理（表单配置式，非 BPMN 拖拽设计器）：模板列表、基本信息、串行审批节点配置、审批人规则（固定人员/指定角色/发起人自选）
- 审批待办中心：待办列表、已办列表、未读标记
- 任务处理：同意、拒绝（必填原因）、退回（退回到发起人）、审批意见
- 审批发起：从业务单据触发、预览审批链
- 进度追踪：我的申请列表、审批节点时间线、历史记录
- 状态变更通知（通过/拒绝/退回 → 推送给申请人；新任务 → 推送给审批人）

P1（核心稳定后迭代）：
- 转交、加签
- 条件分支（按表单字段值自动路由）
- 撤回（申请人主动撤回）、催办
- 批量审批

P2（暂不做）：
- 模板版本管理
- 模拟测试
- 监控大盘
- IM 消息卡片（钉钉/企业微信集成）

**关键技术决策（参考大纲第十节待决策点）**

| 问题 | 新产品决策 |
|------|-----------|
| 流程模板设计器 | 表单配置式（参考飞书风格），不做 BPMN 可视化设计器 |
| 底层引擎 | 保留 Flowable |
| IM 集成 | 暂不做，站内通知覆盖 |
| 退回方式 | 先只做"退回到发起人"，P1 再支持退回到指定节点 |
| 回调机制 | 同步回调（简单可靠，业务量不大时够用） |
| 跨租户模板 | 租户独立配置，不共享 |

**前置依赖（必须先完成）**：用户管理 + 组织架构（审批人规则"直属上级"/"部门负责人"需要组织树）、通知系统

**开发工作量估算：约 8～10 天**

---

### 9.3 低代码图表

**设计参考**：融合旧产品「动态透视宽表框架」和「需求优先级字段配置化」两份设计文档的核心思想，在新产品中用更简洁的方式实现。

**核心设计原则：VIEW 白名单 + 配置驱动查询 + 用户无需写 SQL**

```
业务表（随业务迭代变化）
    ↓ 开发人员维护
语义 VIEW（稳定接口层，每个业务域一个或多个）
    ↓ 注册到白名单
nscp_chart_datasource（VIEW 注册表，安全白名单）
    ↓ 挂载维度/指标/筛选字段定义
配置驱动查询引擎（按白名单字段生成安全 SQL）
    ↓
图表渲染
```

参考旧产品 `rpt_table_registry` 的白名单思想：只有注册过的 VIEW 才能被图表引用，后端生成查询时表名和列名均做白名单校验，彻底消除注入风险。

**用户操作路径（无需写 SQL）**

```
选数据主题（如"需求预测"）
  → 选维度（时间/产品/库存点）       ← 来自 nscp_chart_dimension 白名单
  → 选指标（预测数量/达成率）         ← 来自 nscp_chart_metric 白名单
  → 选筛选条件（时间范围/产品类型）    ← 来自 nscp_chart_filter 白名单
  → 选图表类型（折线/柱状/饼图/数据表）
  → 系统生成安全查询 → 渲染图表
  → 保存 → 拖拽组合成看板
```

**数据表设计（参考旧产品 rpt_* 系列表结构）**

```sql
-- VIEW 注册表（安全白名单，开发人员维护）
-- 参考：rpt_table_registry
nscp_chart_datasource
  id, tenant_id, code, name,
  view_name(对应数据库VIEW名，保存时校验格式只允许字母数字下划线),
  description, module_code(所属模块), status, sort_order

-- 维度字段池（行区域，用户选择"按什么分组"）
-- 参考：rpt_dimension_field
nscp_chart_dimension
  id, datasource_id, col_name(VIEW中已有的列名),
  display_name, dim_type(TIME/TEXT/ENUM/NUMBER),
  enum_class(有枚举时框架自动翻译), is_default, sort_order, is_enabled

-- 指标字段池（值区域，对哪个数值列做什么聚合）
-- 参考：rpt_measure_field
nscp_chart_metric
  id, datasource_id, col_name, display_name,
  agg_func(SUM/COUNT/AVG/MAX/MIN), unit, sort_order, is_enabled

-- 筛选字段池（筛选区域）
-- 参考：rpt_filter_field
nscp_chart_filter
  id, datasource_id, col_name, display_name,
  field_type(ENUM/TEXT/DATE/NUMBER/DATE_RANGE),
  enum_class, is_required, sort_order, is_enabled

-- 图表配置（用户保存的图表）
nscp_chart_config
  id, tenant_id, name, datasource_id,
  chart_type(LINE/BAR/PIE/TABLE/AREA),
  dimensions(JSON, 选中的维度列名列表),
  metrics(JSON, 选中的指标列名列表),
  filter_values(JSON, 默认筛选条件),
  sort_config(JSON),
  created_by, created_at

-- 看板
nscp_chart_dashboard
  id, tenant_id, name, description, is_public,
  created_by, created_at

-- 看板-图表关联（含位置/尺寸）
nscp_chart_dashboard_item
  id, dashboard_id, chart_id,
  position(JSON: {x, y, w, h})
```

**VIEW 命名规范与开发契约**

每完成一个业务域，同步创建对应的语义 VIEW，并注册到 `nscp_chart_datasource`：

```sql
-- 示例：需求预测数据主题 VIEW
CREATE VIEW v_chart_demand_forecast AS
SELECT
  h.tenant_id,
  DATE_FORMAT(h.plan_date, '%Y-%m')  AS time_month,
  p.product_code,
  p.product_name,
  sp.stock_point_code,
  sp.stock_point_name,
  l.forecast_qty,
  h.version_code
FROM nscp_forecast_header h
  JOIN nscp_forecast_line l    ON l.header_id = h.id AND l.deleted = 0
  JOIN nscp_product p          ON p.id = l.product_id AND p.deleted = 0
  JOIN nscp_stock_point sp     ON sp.id = l.stock_point_id AND sp.deleted = 0
WHERE h.deleted = 0;
-- 注意：VIEW 中不加 tenant_id 过滤，由后端 Hibernate 租户约束统一处理
```

**后端安全查询生成（参考旧产品聚合引擎 SQL 生成逻辑）**

```
1. 用户请求带：datasource_code + 选中维度列表 + 选中指标列表 + 筛选值
2. 后端从 nscp_chart_dimension / nscp_chart_metric 加载白名单
3. 校验用户选中的每个字段 ∈ 白名单（不在白名单直接拒绝）
4. 从 nscp_chart_datasource 取 view_name（再次校验格式）
5. 生成 SQL：
   SELECT [白名单维度列], [AGG_FUNC(白名单指标列)] FROM [已注册VIEW]
   WHERE tenant_id = #{tenantId}  ← Hibernate 租户约束统一注入/拼装
   [AND 用户筛选值，全部 #{} 参数化]
   GROUP BY [白名单维度列]
   LIMIT 10000
6. 执行，返回图表数据
```

关键安全保障：表名来自白名单表（不信任用户输入），列名来自白名单表（不信任用户输入），筛选值全部参数化（不拼字符串）。

**与旧产品设计的主要简化点**

| 旧产品（动态透视宽表框架） | 新产品 |
|--------------------------|--------|
| 双引擎（聚合引擎 + 指标树引擎） | 单引擎（聚合类），供需平衡类暂不做 |
| 聚合引擎直接拼 UNION ALL + 动态建表 | VIEW 作为数据源，取数逻辑收敛到 VIEW 里 |
| SPI DataProvider + FormulaRegistry | VIEW 内部处理数据逻辑，不需要 SPI |
| rpt_table_registry（物理表注册） | nscp_chart_datasource（VIEW 注册，更安全） |
| 支持实施顾问配置+扫描物理表 | 由开发人员维护 VIEW 注册，更简单 |

**开发工作量估算：约 8～10 天**（图表配置引擎 + 看板拖拽布局）

---

### 9.4 配置驱动设计原则

> 新产品的核心设计思想：**用户能在系统界面完成绝大部分配置，不需要修改代码**

| 层级 | 配置驱动体现 |
|------|-------------|
| 系统层 | 枚举/字典值、系统参数、通知模板 → 管理员在界面配置 |
| 用户权限层 | 用户、角色、权限 → 管理员在界面配置 |
| 组织层 | 部门、上下级关系 → 管理员在界面配置（供审批流"直属上级"规则使用） |
| 审批层 | 审批模板、审批节点、审批人规则 → 管理员在界面配置（Flowable 模板由系统动态生成） |
| 图表层 | 数据主题、维度、指标、图表、看板 → 用户在界面配置 |
| 业务配置层 | 安全库存计算规则、优先级因子权重、净需求规则 → 用户在界面配置 |
| 业务数据层 | 订单、预测等核心数据的录入和维护 → 用户操作 |

**实现原则**：
- 枚举值（状态、类型、分类）存数据库，不写死代码，管理员可增删改
- 业务规则（计算方式、权重、阈值）存配置表，不写死计算逻辑里
- 审批规则（谁审、几级）存 Flowable 配置，不写死流程定义
- 只有图表 VIEW 的注册需要开发介入（因为 VIEW 本身是 SQL，不可避免）

---

### 9.5 与本章其余小节的关系

分阶段工期、依赖关系与阶段 0～12 的详细说明已统一收拢至 **第四章「开发计划」**（4.1 依赖图、4.2 分阶段说明、4.3 总工期表）。本章 **9.1～9.4** 分别从方案层展开通知、审批、低代码图表与配置驱动原则，实施时与第四章对应阶段对齐即可。

---

### 8.1 隔离模式选择

三种常见模式对比：

| 模式 | 说明 | 适合场景 |
|------|------|----------|
| 独立数据库 | 每个租户一个 DB | 数据安全要求极高，成本高 |
| 共享 DB + 独立 Schema | 每个租户一个 Schema | 中等隔离，MySQL 支持不好 |
| **共享 DB + 行级隔离（推荐）** | 所有租户同表，靠 tenant_id 区分 | SaaS 早期标准做法，成本低，扩展方便 |

**决策：使用行级隔离（shared database, shared schema）**。未来数据量大或有客户要求独立时，再升级到独立数据库模式。

---

### 8.2 数据库层：每张表都有 tenant_id

```sql
-- 所有业务表公共字段（必须，不可省略）
id          BIGINT          NOT NULL AUTO_INCREMENT PRIMARY KEY,
tenant_id   VARCHAR(64)     NOT NULL COMMENT '租户ID',
created_by  VARCHAR(64)     NOT NULL,
created_at  DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
updated_by  VARCHAR(64),
updated_at  DATETIME        ON UPDATE CURRENT_TIMESTAMP,
deleted     TINYINT(1)      NOT NULL DEFAULT 0 COMMENT '逻辑删除'
```

**联合索引**：高频查询字段 + tenant_id 必须建联合索引，例如：
```sql
INDEX idx_tenant_product (tenant_id, product_code)
```

---

### 8.3 后端层：Hibernate 租户约束统一注入

使用 Hibernate（Spring Data JPA）统一处理租户约束，目标是**业务查询默认带 `tenant_id` 限制**，而不是由业务代码手写散落 `WHERE tenant_id = ?`。

推荐实现（可组合）：
- 请求上下文 `TenantContext` 提供当前租户
- Repository 基类/Specification 统一拼装 `tenant_id` 条件
- 对关键实体开启 Hibernate Filter（需要时）
- 超级管理员能力通过显式租户切换 + 白名单服务控制，而不是默认全放开

```java
public abstract class TenantScopedService {
    protected String currentTenantId() {
        return TenantContext.getTenantId();
    }
}
```

---

### 8.4 后端层：tenant_id 的传递链路

```
客户端请求
  → 携带 JWT Token（token 里包含 tenant_id）
  → 后端 Filter/Interceptor 解析 JWT
  → 把 tenant_id 存入 ThreadLocal（TenantContext）
  → Hibernate 层统一按 tenant_id 约束查询
  → 请求结束时清除 ThreadLocal（防止内存泄漏）
```

---

### 8.5 前端层：租户信息的处理

- 登录接口返回 JWT，前端存在 `localStorage`（或 `sessionStorage`）
- 每次请求通过 Axios 拦截器自动在 Header 加 `Authorization: Bearer <token>`
- **前端不需要手动传 tenant_id**，后端从 token 里解析

---

### 8.6 租户注册/管理（最简实现）

实验阶段只需要一张简单的租户表：

```sql
CREATE TABLE nscp_sys_tenant (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id   VARCHAR(64) NOT NULL UNIQUE COMMENT '租户唯一标识（UUID）',
    tenant_name VARCHAR(128) NOT NULL COMMENT '租户名称/公司名',
    status      TINYINT(1) NOT NULL DEFAULT 1 COMMENT '1启用 0禁用',
    created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

早期手动在表里插一条记录就可以用，不需要做租户注册页面。

---

### 8.7 注意事项

- **tenant_id 用 UUID 字符串，不用自增 ID**：防止枚举攻击（攻击者猜测其它租户的 ID）
- **超级管理员跨租户操作必须显式指定目标租户**：通过服务层白名单与租户切换控制，不允许隐式全局绕过
- **不同租户的数据量差异大时**：单张表数据量大了可以按 tenant_id 分区（后期优化，现在不用考虑）




